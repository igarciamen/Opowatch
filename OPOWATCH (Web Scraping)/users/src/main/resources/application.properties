spring.application.name=users
server.port=8081

# PostgreSQL BBDD
spring.datasource.url=jdbc:postgresql://localhost:5432/usersOpoWatch
spring.datasource.username=postgres
spring.datasource.password=apertura15

# Hibernate configuration (database management)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public

# JWT Security
jwt.secret=0123456789ABCDEFGHIJKLMNOPQRSTUV
jwt.expirationMs=86400000

# Admin parameters
app.admin.username=isabel
app.admin.password=123456

internal.api-key=change-me-internal-key-2026